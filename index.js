#!/usr/bin/env node
import colors from 'colors';

console.log(`
${colors.magenta(
  `*********** | ${colors.bold(colors.white('Slekup'))} | ***********`
)}

${colors.green('Website')}: ${colors.blue('https://slekup.com')}
${colors.green('GitHub')}: ${colors.blue('https://github.com/slekup')}
`);
